<div class="navigation">
    <div class="logo">🙅🏻🏌🏻‍♂️</div>
    <div class="buttons">
        <form>
            <button class="login" formaction="/login">LOG IN</button>
            <button class="home" formaction="/home">HOME</button>
        </form>
    </div>
</div>

<div class="content">
    <div class="profile">
        {{#if err}}
            <h4>{{err}}</h4>
        {{/if}}

        <div class="textbox">
            <p>Enter a new password. 🤫</p>
        </div>

        <form class="register" method="POST" autocomplete="off">
            <input class="standard" name="password" type="password" />
            <div>Password</div>
            <input class="standard" name="password2" type="password" />
            <div>Enter your password again</div>
            <button class="submit" id="submit">SUBMIT</button>
        </form>
    </div>
</div>