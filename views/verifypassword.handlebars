<div class="navigation">
    <div class="logo">🙅🏻🏂</div>
    <div class="buttons">
        <form>
            <button class="profile" formaction="/profile">PROFILE</button>
            <button class="home" formaction="/home">HOME</button>
        </form>
    </div>
</div>

<div class="content">
    <div class="profile">
        {{#if err}}
            <h4 class="err">{{err}}</h4>
        {{/if}}

        <div class="textbox">
            <p>Verify your password to edit profile 👀.</p>
        </div>

        <form class="register" method="POST" autocomplete="off">
            <input class="standard" name="password" type="password" />
            <div>Password</div>
            <button class="verify" id="verify">VERIFY</button>
        </form>
    </div>
</div>